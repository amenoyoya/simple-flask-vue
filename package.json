{
  "devDependencies": {
    "browser-sync": "^2.26.5",
    "gulp": "^4.0.2",
    "npm-run-all": "^4.1.5"
  },
  "scripts": {
    "server": "python server.py",
    "sync": "gulp",
    "dev": "npm-run-all --parallel server sync"
  }
}
